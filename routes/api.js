const express = require('express');
const router = express.Router();
router.get('/todos', (req, res, next) => {
  res.json({
    "_id": "0ax3247382hj2k3h42",
    "name": "sne6593z_lidar"
  });
});

module.exports = router;
